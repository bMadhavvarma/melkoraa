'use client'
import { useState } from 'react'
const questions = [['How do MELKORAA pieces fit?', 'Our silhouettes are designed for an easy, everyday fit. Each product page includes details to help you choose.'], ['How do I care for my pieces?', 'Follow the care label on each garment. We recommend washing thoughtfully and letting pieces air dry.'], ['Can I change my order?', 'If your order has not shipped, contact our care team as soon as possible and we will do our best to help.'], ['Where do you ship?', 'We currently ship across India. More destinations are coming soon.']]
export default function FAQPage() { const [open, setOpen] = useState(0); return <main className="content-page faq-page"><p className="eyebrow">NEED TO KNOW</p><h1>Frequently<br />asked.</h1><section className="faq-list">{questions.map(([question, answer], index) => <div className="faq-item" key={question}><button onClick={() => setOpen(open === index ? -1 : index)} aria-expanded={open === index}><span>{question}</span><b>{open === index ? '−' : '+'}</b></button>{open === index && <p>{answer}</p>}</div>)}</section></main> }
